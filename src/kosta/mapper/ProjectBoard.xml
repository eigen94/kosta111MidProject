<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.mapper.ProjectBoardMapper">
<cache/>
	<insert id="insertProject" parameterType="ProjectBoard">
		INSERT INTO project(
			P_ID, P_NAME, P_START, P_END, P_MEMO
		)VALUES(
			PROJECT_SEQ.NEXTVAL, #{p_name }, #{p_start }, #{p_end }, #{p_memo } 
		)
	</insert>

</mapper>