<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.loginMapper.loginMapper">
<cache/>
	<insert id="insertMember" parameterType="Member">
		insert into member
			(m_id, m_name, m_email m_pwd,m_phone, m_question, m_answer, b_freind)
			values(
			MVL((select max(m_id) from member)+1,0), #{m_name},#{m_email},#{m_pwd},#{m_phone},#{m_question},#{m_answer},#{m_freind}
			)
	</insert>
	
	
</mapper>