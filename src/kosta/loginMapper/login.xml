<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.loginMapper.loginMapper">
<cache/>
	<select id="kosta.login.Member" parameterType="Member" >
	select * from member where m_id = #{m_id} and m_pwd =#{m_pwd}
	</select>
	<insert id="insertMember" parameterType="Member">
		insert into member
			(m_id, m_name, m_email m_pwd,m_phone, m_question, m_answer)
			values(
			MVL((select max(m_id) from member)+1,0), #{m_name},#{m_email},#{m_pwd},#{m_phone},#{m_question},#{m_answer}
			)
	</insert>
	
	
</mapper>