<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kosta.mapper.ProjectBoardMapper">
<cache/>


<select id="projectList" resultType="ProjectBoard">
	select * from project
</select>

	<insert id="insetProjectBoard" parameterType="ProjectBoard">
		INSERT INTO project(
			P_ID, P_NAME, P_START, P_END, P_MEMO
		)VALUES(
			PROJECT_SEQ.NEXTVAL, #{p_name }, sysdate, sysdate, #{p_memo } 
		)
	</insert>


</mapper>
